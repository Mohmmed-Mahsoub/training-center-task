const AddCourse = () => {
  return (
    <section
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1>addCourse</h1>
    </section>
  );
};

export default AddCourse;
