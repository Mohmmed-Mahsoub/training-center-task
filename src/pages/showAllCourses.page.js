const ShowAllCourses = () => {
  return (
    <section
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1>ShowAllCourses</h1>
    </section>
  );
};

export default ShowAllCourses;
