const ShowStudent = () => {
  return (
    <section
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1>ShowStudent</h1>
    </section>
  );
};

export default ShowStudent;
