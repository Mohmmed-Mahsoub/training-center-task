const ShowCourse = () => {
  return (
    <section
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1>ShowCourse</h1>
    </section>
  );
};

export default ShowCourse;
