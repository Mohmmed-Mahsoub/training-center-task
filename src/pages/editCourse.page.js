const EditCourse = () => {
  return (
    <section
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1>EditCourse</h1>
    </section>
  );
};

export default EditCourse;
